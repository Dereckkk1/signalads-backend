/**
 * Cron Job - Backup automatico do MongoDB Atlas
 * TODO: Reimplementar backup sem dependencia de scripts_bak
 */

export function startBackupCron(): void {
  // Backup cron desabilitado — script de backup removido durante limpeza de dead code
  // Para reativar, reimplementar logica de backup com mongodump
}
